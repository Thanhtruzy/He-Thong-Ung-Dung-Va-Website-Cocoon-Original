name: my_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_staggered_grid_view: ^0.4.0
  http: ^1.2.2
  shared_preferences: ^2.0.15
  cupertino_icons: ^1.0.8
  intl: ^0.18.0
  flutter_svg: ^0.22.0
  badges: ^2.0.2
  cached_network_image: ^3.1.0
  carousel_slider: ^5.0.0  # Change the version as needed

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  animate_do: ^3.3.4

flutter:

  uses-material-design: true

  assets:
    - assets/icons/
    - assets/images/
