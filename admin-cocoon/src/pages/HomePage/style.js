import { styled } from "styled-components";

export const TextHeader = styled.span`
    color:#000;
    font-family: Nunito;
    text-align: center;
    cursor: pointer;
    &:hover {
        text-decoration: underline;
    }
`